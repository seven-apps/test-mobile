import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  tag: {
    textAlign: 'center',
    backgroundColor: '#777',
    margin: 20,
    color: '#fff',
    lineHeight: 25
  }
})

export default styles
